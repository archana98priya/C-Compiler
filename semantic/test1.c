#include <stdio.h>

// int foo(int a, int b)
// {
//   return 5;
// }
int foo(int a, int b){
  return 1;
}

int main() {
  int a[6];
  foo(1, 1);
  int d , e;

  d+e = 1+2;

    /*///for loop *****/
    for(a=a+1; a<(16+4); a=a+1+a) {
        // printf("Value of a: %d\n", a);
        a+=1.0; // To be changed

        if(a)
         a++;
    }
    return 0;
}